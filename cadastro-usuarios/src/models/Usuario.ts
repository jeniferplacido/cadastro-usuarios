// Define uma interface chamada Usuario para tipagem no TypeScript
export interface Usuario {
    id: number; // Define uma propriedade 'id' do tipo number
    nome: string; // Define uma propriedade 'nome' do tipo string
    email: string; // Define uma propriedade 'email' do tipo string
    senha: string; // Define uma propriedade 'senha' do tipo string
}
